import { Typography } from "@/components";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ProfileScreen() {
  return (
    <SafeAreaView>
      <Typography variant="body" style={{ padding: 8 }}>
        This page is under construction
      </Typography>
    </SafeAreaView>
  );
}
